// lib/screens/activity/activity_form_screen.dart (placeholder)
import 'package:flutter/material.dart';

class ActivityFormScreen extends StatelessWidget {
  const ActivityFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Tạo hoạt động mới'),
        backgroundColor: Colors.blue.shade700,
        foregroundColor: Colors.white,
      ),
      body: const Center(
        child: Text('Activity Form Screen'),
      ),
    );
  }
}